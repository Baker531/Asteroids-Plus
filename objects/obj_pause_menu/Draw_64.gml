if (!running) return;

scribble("[fnt_head][c_yellow]Paused").draw(50, 50)
scribble("Press P to unpause").draw(50, 100)
scribble("Press E to exit to instructions").draw(50, 140)
scribble("Press Q to quit").draw(50, 180)